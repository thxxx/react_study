{
    "compilerOptions": {
        "module": "commonjs",
        "target": "ES2015", // 어떤 버전의 자바스크립트로 컴파일 되고 싶은지.
        "sourceMap" : true,
        "outDir":"dist"
    },
    "include": [ // 어떤 파일들이 컴파일 과정에 포함되는지 타입스크립트에게 알려줌.
    // 컴파일 과정에서 포함할 파일의 배열을 적으면 됨
        "src/**/*"
    ],
    "exclude": [
        "node_modules" //  항상 한다?
    ]
}